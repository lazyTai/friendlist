import {MyMap} from "../../util/lodash";

export default {
    "a": MyMap([1, 2, 3, 4,], item => {
        return {id: `a${item}`, name: `a${item}`}
    }),
    "b": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `b${item}`, name: `b${item}`}
    }),
    "c": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `c${item}`, name: `c${item}`}
    }),
    "d": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `d${item}`, name: `d${item}`}
    }),
    "e": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `e${item}`, name: `e${item}`}
    }),
    "f": MyMap([1, 2, 3, 4, 5, 8], item => {
        return {id: `f${item}`, name: `f${item}`}
    }),
    "g": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `g${item}`, name: `g${item}`}
    }),
    "h": MyMap([1, 2, 3, 5, 6, 7, 8], item => {
        return {id: `h${item}`, name: `h${item}`}
    }),
    "i": MyMap([1, 2, 3,, 6, 7, 8], item => {
        return {id: `i${item}`, name: `i${item}`}
    }),
    "j": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `j${item}`, name: `j${item}`}
    }),
    "k": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `k${item}`, name: `k${item}`}
    }),
    "l": MyMap([1, 2, 3, 4, 5,, 8], item => {
        return {id: `l${item}`, name: `l${item}`}
    }),
    "m": MyMap([1, 2, 3, 4, 5, 7, 8], item => {
        return {id: `m${item}`, name: `m${item}`}
    }),
    "n": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `n${item}`, name: `n${item}`}
    }),
    "o": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `o${item}`, name: `o${item}`}
    }),
    "p": MyMap([1, 2, 3,  6, 7, 8], item => {
        return {id: `p${item}`, name: `p${item}`}
    }),
    "q": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `q${item}`, name: `a${item}`}
    }),
    "r": MyMap([1, 2], item => {
        return {id: `r${item}`, name: `r${item}`}
    }),
    "s": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `s${item}`, name: `s${item}`}
    }),
    "t": MyMap([1, 2, 3, 4, 5, 8], item => {
        return {id: `t${item}`, name: `t${item}`}
    }),
    "u": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `u${item}`, name: `u${item}`}
    }),
    "v": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `v${item}`, name: `v${item}`}
    }),
    "w": MyMap([1, 2, 3,  7, 8], item => {
        return {id: `w${item}`, name: `w${item}`}
    }),
    "x": MyMap([1, 2, 3, 4, 5, 6, 7, 8], item => {
        return {id: `x${item}`, name: `x${item}`}
    }),
    "y": MyMap([1, 2, 3,  7, 8], item => {
        return {id: `y${item}`, name: `y${item}`}
    }),
    "z": MyMap([1, 2, 7, 8], item => {
        return {id: `z${item}`, name: `z${item}`}
    }),

}